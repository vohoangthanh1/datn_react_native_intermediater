import { StyleSheet } from "react-native";

export const DescriptionProductStyles = StyleSheet.create({
    title: {
        textAlign: "center",
        color: "black",
        fontWeight: "500",
        fontSize: 20,
        marginTop: 10
    }
})
